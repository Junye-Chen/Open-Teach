defaults:
  - _self_
  - network
  - camera
  - robot: piper
  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled  

 
use_sensor: true

hydra:  
  output_subdir: null  
  run:  
    dir: .